<h1><%= @artwork.name %></h1>

<img src="">
<p><%= @artwork.artist %></p>
<p><%= @artwork.category %></p>
<p><%= @artwork.description %></p>

<a><%= link_to "Remove this artwork", artwork_path(@artwork), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></a>

<%= link_to "Back to Gallery", artworks_path %>
